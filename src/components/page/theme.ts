// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1877f2', // Cor principal do Facebook
    },
  },
});

export default theme;
